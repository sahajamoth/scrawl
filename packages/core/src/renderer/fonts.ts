// Google Fonts URL for web environments (lightweight, no self-hosting required for v1).
// In v2, we will prebake a base64 WOFF2 subset for fully offline SVGs.
export const FONT_STYLE = `@import url('https://fonts.googleapis.com/css2?family=Permanent+Marker&display=swap');`

export const FONT_FALLBACK_STYLE = `
  .scrawl-label { font-family: 'Permanent Marker', 'Comic Sans MS', cursive; }
`
